import {NextRequest,NextResponse} from 'next/server';
// The project's default vercel.app alias serves production too; send it to the
// canonical host so search engines only ever index www.monishpatalay.dev.
const ALIAS_HOST='myportfolio-nine-gilt-15.vercel.app',CANONICAL_HOST='www.monishpatalay.dev';
export function proxy(request:NextRequest){if(request.headers.get('host')===ALIAS_HOST){const url=request.nextUrl.clone();url.protocol='https';url.host=CANONICAL_HOST;url.port='';return NextResponse.redirect(url,308);}const nonce=Buffer.from(crypto.randomUUID()).toString('base64');const studio=request.nextUrl.pathname.startsWith('/studio');const dev=process.env.NODE_ENV!=='production';const policy=["default-src 'self'",`script-src 'self' 'nonce-${nonce}' ${dev?"'unsafe-eval' https://va.vercel-scripts.com":''} ${studio?'https://core.sanity-cdn.com':''}`,"style-src 'self' 'unsafe-inline'","img-src 'self' data: blob: https://cdn.sanity.io https://image.mux.com","media-src 'self' blob: https://cdn.sanity.io https://stream.mux.com",`connect-src 'self' https://*.sanity.io https://*.api.sanity.io https://*.mux.com https://storage.googleapis.com https://*.litix.io https://vitals.vercel-insights.com https://va.vercel-scripts.com ${dev?'ws:':''}`,`font-src 'self' data: ${studio?'https://design-system-static.sanity.io':''}`,"worker-src 'self' blob:","object-src 'none'","base-uri 'self'","frame-ancestors 'none'",studio?'frame-src https://*.sanity.io':"frame-src 'none'"].join('; ');const headers=new Headers(request.headers);headers.set('x-nonce',nonce);headers.set('Content-Security-Policy',policy);const response=NextResponse.next({request:{headers}});response.headers.set('Content-Security-Policy',policy);return response;}
export const config={matcher:['/((?!api|_next/static|_next/image|favicon.ico|images|models|.*\\.(?:svg|png|webp|avif|woff2)$).*)']};
